# Environment Variables for CI/CD Secret Management
# 
# This file documents required environment variables for automated secret injection.
# Set these environment variables in your CI/CD pipeline or local environment.
#
# Usage (CI/CD Only):
#   1. Set environment variables (see mappings below)
#   2. Generate .env.ssm: ./scripts/bootstrap/helpers/generate-env-ssm.sh
#   3. Inject to SSM: ./scripts/bootstrap/install/08-inject-ssm-parameters.sh
#
# Environment Variable → SSM Parameter Mapping:
# (Set these as environment variables, not in this file)

# ============================================================================
# Global LLM API Keys (Optional - for AI services)
# ============================================================================
# Environment variable: OPENAI_API_KEY → /zerotouch/prod/openai_api_key
# Environment variable: ANTHROPIC_API_KEY → /zerotouch/prod/anthropic_api_key

# ============================================================================
# AWS Credentials for AgentSandbox S3 Operations (Required)
# ============================================================================
# Environment variable: AWS_ACCESS_KEY_ID → /zerotouch/prod/aws-access-key-id
# Environment variable: AWS_SECRET_ACCESS_KEY → /zerotouch/prod/aws-secret-access-key
# Environment variable: AWS_REGION → /zerotouch/prod/aws-default-region

# ============================================================================
# Hetzner Cloud Credentials (Required for Public Gateway)
# ============================================================================
# Environment variable: HETZNER_API_TOKEN → /zerotouch/prod/hetzner/api-token
# Environment variable: HETZNER_DNS_TOKEN → /zerotouch/prod/hetzner/dns-token

# ============================================================================
# GitHub Credentials (Required)
# ============================================================================
# Environment variable: BOT_GITHUB_USERNAME → /zerotouch/prod/ghcr/username, /zerotouch/prod/github/username
# Environment variable: BOT_GITHUB_TOKEN → /zerotouch/prod/ghcr/password, /zerotouch/prod/github/token

# ============================================================================
# ArgoCD Repository Credentials (Required)
# ============================================================================
# Environment variable: TENANTS_REPO_URL → /zerotouch/prod/argocd/repos/zerotouch-tenants/url
# Environment variable: BOT_GITHUB_USERNAME → /zerotouch/prod/argocd/repos/zerotouch-tenants/username
# Environment variable: BOT_GITHUB_TOKEN → /zerotouch/prod/argocd/repos/zerotouch-tenants/password

# ============================================================================
# IDE Orchestrator Service Secrets (Required for ide-orchestrator service)
# ============================================================================
# Environment variable: JWT_SECRET → /zerotouch/prod/ide-orchestrator/jwt-secret
# Environment variable: SPEC_ENGINE_URL → /zerotouch/prod/ide-orchestrator/spec-engine-url

# ============================================================================
# Identity Service Secrets (Required for identity-service)
# ============================================================================
# Environment variable: IDENTITY_OIDC_ISSUER → /zerotouch/prod/identity-service/oidc_issuer
# Environment variable: IDENTITY_OIDC_CLIENT_ID → /zerotouch/prod/identity-service/oidc_client_id
# Environment variable: IDENTITY_OIDC_CLIENT_SECRET → /zerotouch/prod/identity-service/oidc_client_secret
# Environment variable: IDENTITY_OIDC_REDIRECT_URI → /zerotouch/prod/identity-service/oidc_redirect_uri
# Environment variable: IDENTITY_ALLOWED_REDIRECT_URIS → /zerotouch/prod/identity-service/allowed_redirect_uris
# Environment variable: IDENTITY_DATABASE_URL → /zerotouch/prod/identity-service/database_url
# Environment variable: IDENTITY_JWT_PRIVATE_KEY → /zerotouch/prod/identity-service/jwt_private_key
# Environment variable: IDENTITY_JWT_PUBLIC_KEY → /zerotouch/prod/identity-service/jwt_public_key
# Environment variable: IDENTITY_JWT_KEY_ID → /zerotouch/prod/identity-service/jwt_key_id
# Environment variable: IDENTITY_JWT_PREVIOUS_PRIVATE_KEY → /zerotouch/prod/identity-service/jwt_previous_private_key
# Environment variable: IDENTITY_JWT_PREVIOUS_PUBLIC_KEY → /zerotouch/prod/identity-service/jwt_previous_public_key
# Environment variable: IDENTITY_JWT_PREVIOUS_KEY_ID → /zerotouch/prod/identity-service/jwt_previous_key_id
# Environment variable: IDENTITY_TOKEN_PEPPER → /zerotouch/prod/identity-service/token_pepper
