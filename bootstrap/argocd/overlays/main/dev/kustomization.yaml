apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../core
- ./secrets
- 99-tenants.yaml

patches:
- path: gateway-certificate-patch.yaml
  target:
    kind: Certificate
    name: nutgraf-tls-cert
