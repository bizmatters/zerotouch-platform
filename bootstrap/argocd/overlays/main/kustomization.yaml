apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Main overlay - use core resources without tenant-specific configs
# For environment-specific deployments, use: dev/, staging/, production/
resources:
- core