apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-dns
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://kubernetes-sigs.github.io/external-dns
    chart: external-dns
    targetRevision: "1.15.0"
    helm:
      values: |
        provider: webhook
        extraArgs:
          - --webhook-provider-url=http://localhost:8888
        sources:
          - gateway-httproute
        domainFilters:
          - nutgraf.in
        sidecars:
          - name: hetzner-webhook
            image: ghcr.io/angelnu/external-dns-hetzner-webhook:latest
            ports:
              - containerPort: 8888
                name: webhook
            env:
              - name: HETZNER_API_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: external-dns-hetzner
                    key: HETZNER_DNS_TOKEN
              - name: SERVER_PORT
                value: "8888"
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            cpu: 50m
            memory: 64Mi
        logLevel: info
        policy: sync
        registry: txt
        txtOwnerId: external-dns
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 15
      backoff:
        duration: 30s
        maxDuration: 10m