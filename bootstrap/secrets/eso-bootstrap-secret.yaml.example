# ESO Bootstrap Secret
# This is the ONE manual secret needed to bootstrap External Secrets Operator
# 
# Instructions:
# 1. Create a GitHub Personal Access Token (PAT) with 'repo' scope
# 2. Copy this file: cp eso-bootstrap-secret.yaml.example eso-bootstrap-secret.yaml
# 3. Replace YOUR_GITHUB_PAT with your actual token
# 4. Apply: kubectl apply -f eso-bootstrap-secret.yaml
# 5. DO NOT commit the actual secret file (it's in .gitignore)

apiVersion: v1
kind: Secret
metadata:
  name: github-access-token
  namespace: external-secrets
type: Opaque
stringData:
  token: YOUR_GITHUB_PAT

# sample command
cat > /tmp/eso-bootstrap-secret.yaml << 'EOF'
apiVersion: v1
kind: Secret
metadata:
  name: github-access-token
  namespace: external-secrets
type: Opaque
stringData:
  token: YOUR_GITHUB_PAT
EOF
kubectl apply -f /tmp/eso-bootstrap-secret.yaml